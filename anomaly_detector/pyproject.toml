[project]
name = "atmospheric-anomaly-detector"
version = "0.1.0"
description = "MQTT-based environmental sensor anomaly detector"
requires-python = ">=3.12"
dependencies = [
    "paho-mqtt>=2.1,<3",
    "scikit-learn>=1.7,<2",
]
